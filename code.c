#include <stdio.h>

int main() {
  int x = 1;
  int y = 2;
  int z = x + y;
  printf("%d\n", z);

  // added this for new commit on input-checking branch
  // added this for new commit on user-settings
}
